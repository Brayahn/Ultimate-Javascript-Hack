/**
 Arrow Functions
 */

// do not have this reserved keyword
//the parenthisis can be ignored if there's one parameter
const square = (number) => {
  return number * number;
};

let result = square(50);
console.log(result);

//Short Coincise version //if there's one return statement
//the parenthisis can be ignored if there's one parameter
const add = (number) => number + number;

const result2 = add(4);
console.log(result2);
